---
description: Append a one-line session entry to CLAUDE.local.md
---

Append a single line to the "Session log" section of `CLAUDE.local.md`.

Format: `YYYY-MM-DD — phase N — <what we worked on> — <where we left off / next step>`

Keep it to one line. No more than ~30 words. This file accumulates over weeks; brevity matters.

Read the existing log first to keep the format consistent. Don't add headers, separators, or commentary — just the line.
