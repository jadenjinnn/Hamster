---
description: Re-read code areas relevant to recent changes and update docs/architecture.md
---

Re-read the codebase areas relevant to recent changes (use `git diff` and `git log` since the last architecture update to scope). Update `docs/architecture.md` to reflect the current state.

Constraints:
- Show me the diff of `docs/architecture.md` before saving.
- Do not invent details. If something is unclear, list it under the "Open questions" section instead of guessing.
- Preserve the template structure in `.claude/templates/architecture.md`.
- Keep prose tight. This doc is read often; bullets and short paragraphs over walls of text.
