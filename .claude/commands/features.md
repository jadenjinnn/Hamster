---
description: Status of all in-flight feature specs.
---

List the contents of `docs/features/active/`. For each spec file, report:

- **Name**
- **Tier** (sketch / design / full spec)
- **Status** (draft / approved / in progress)
- **Why-this-approach written?** (yes / no)
- **Started** (date)
- **Last update** (from git log on the file)

Then check counts:

- If 1 active feature: nothing to flag, "one feature in flight."
- If 2 active features: note both, ask if I want to focus on one.
- If 3+ active features: flag this. Quote the workflow doc: "If three or more accumulate, suggest a feature freeze: pick one to finish, move the others to `docs/features/parked/`." Recommend a freeze and ask which to keep active.

Keep the output to a single screen.
